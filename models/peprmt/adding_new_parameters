# 1. Add to R/write.config.PEPRMT.R

# 2. Source that file, or push to GitHub and re-load. Basically, you need to 
# actually get PECAN to incorporate your changes

# 3. Make sure the new parameter set is represented in your PFT files. 
# Currently, there are version of this scattered throughout the repo and it can
# be tricky to make sure the one the model is using is actually updated
# demo_run/set_up_met.qmd has an example for creating these.