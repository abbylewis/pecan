% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ca_n_rate.R
\name{get_ca_n_rate}
\alias{get_ca_n_rate}
\title{Look up California N application rates by crop}
\usage{
get_ca_n_rate(crop, pft_group = NULL, unit = c("kg_ha", "lbs_acre"))
}
\arguments{
\item{crop}{Character string. Crop name to look up.}

\item{pft_group}{Optional character string. Filter results to a specific
plant functional type group (e.g. "row", "woody", "rice").}

\item{unit}{Character, one of "kg_ha" (default) or "lbs_acre". Controls
which columns appear as `min_n` and `max_n` in the output.}
}
\value{
A data frame with columns: `pft_group`, `crop`, `min_n`, `max_n`,
  `source`. The `min_n` and `max_n` columns are in the requested unit.
  Returns an empty data frame (with a warning) if no match is found.
}
\description{
Returns recommended nitrogen application rate ranges for California crops,
based on CDFA-FREP guidelines and UC ANR publications. Rates are provided
in both imperial (lbs N/acre) and SI (kg N/ha) units.
}
\details{
Matching is case-insensitive. Exact matches are returned directly.
If no exact match is found, partial matching is used to suggest
possible crops and an empty data frame is returned.
}
\examples{
get_ca_n_rate("Tomatoes, Processing")
get_ca_n_rate("corn")
get_ca_n_rate("wheat", unit = "lbs_acre")
get_ca_n_rate("pistachio", pft_group = "woody")

}
\seealso{
[look_up_fertilizer_components()] for fertilizer nutrient
  composition (N/C fractions) from the SWAT/DayCent database.
  [ca_n_application_rate] for the underlying dataset.
}
